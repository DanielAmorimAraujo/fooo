import { createMuiTheme } from '@material-ui/core/styles';

export const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#44a1a0',
    },
    secondary: {
      main: '#0d5c63',
    },
  },
  typography: {
    body1: {
      color: '#fffffa',
      fontFamily: 'Alata',
    },
  },
});

// baby pink - #f6caca
